// Convenience re-export so consumers in segmentation-aware code can grab
// `matchesAudience` without reaching into ../audience. Pure data file —
// intentionally untested (see `userguiding-phase-2-tests.md` §"What NOT to Test").
export { matchesAudience } from '../audience'
