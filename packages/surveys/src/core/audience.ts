// Promoted to `@tour-kit/core` in Phase 1 of the refactor train. The surveys
// audience condition shape is byte-identical to the core one, so this is a
// pure re-export — no behavior change.
export { matchesAudience } from '@tour-kit/core'
