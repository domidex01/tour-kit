'use client'

// Export Radix Slot for backward compatibility
export { Slot, Slottable } from '@radix-ui/react-slot'

// Export Unified Slot for Base UI support (canonical source: @tour-kit/core)
export { UnifiedSlot } from '@tour-kit/core'
export type { RenderProp, UnifiedSlotProps } from '@tour-kit/core'
