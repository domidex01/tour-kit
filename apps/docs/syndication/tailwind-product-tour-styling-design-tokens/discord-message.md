## Channel: #articles or #show-off in Reactiflux

**Message:**

Wrote a tutorial on using Tailwind v4's `@theme` directive to style product tours with design tokens. Covers a three-layer token setup, ARIA-tied beacon visibility with `aria-expanded:` variants, and a `prefers-reduced-motion` approach that collapses all animations via CSS variable overrides.

https://usertourkit.com/blog/tailwind-product-tour-styling-design-tokens

Curious if anyone else has set up tour-specific tokens in their Tailwind projects — the motion token part was the most interesting to figure out.
