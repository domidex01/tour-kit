# How to Add a Product Tour to a React 19 App in 5 Minutes

### ref-as-prop targeting, async useTransition steps, and full TypeScript — no forwardRef needed.

*Originally published at [usertourkit.com](https://usertourkit.com/blog/add-product-tour-react-19)*

---

**Import instructions:** Use medium.com/p/import and paste the URL https://usertourkit.com/blog/add-product-tour-react-19 — Medium will auto-set the canonical tag.

**Submit to:** JavaScript in Plain English publication for wider reach.

**Notes for Medium adaptation:**
- Keep code blocks under 20 lines (Medium has no syntax highlighting)
- Remove the HTML table and replace with the prose version below
- Remove JSON-LD schema section

---

> Use the same body content as devto.md, with these changes:
> 1. Replace the React 19 features HTML table with: "userTourKit handles React 19's key changes natively. Ref-as-prop means no forwardRef wrappers for target elements. Async useTransition lets step navigation await route changes without blocking UI. Ref cleanup functions handle spotlight teardown declaratively. Server Components work around the TourProvider (only the provider needs 'use client'). And Context-as-provider gives cleaner syntax without the .Provider suffix."
> 2. Keep code blocks — they're all under 20 lines already
> 3. Remove the FAQ section (too long for Medium) — replace with: "The full FAQ and troubleshooting guide are in the [original article](https://usertourkit.com/blog/add-product-tour-react-19)."
> 4. Keep the troubleshooting section but trim each issue to the explanation only (drop the code blocks for the troubleshooting fixes)
> 5. End with: "The complete tutorial with all code examples, troubleshooting details, and FAQ is in the [original article](https://usertourkit.com/blog/add-product-tour-react-19)."
