## Subject: Tutorial — Building a "What's New" changelog modal in React with native <dialog>

## Recipients:
- Cooperpress (React Status, JavaScript Weekly, Frontend Focus): editor@cooperpress.com
- This Week in React: sebastien@thisweekinreact.com
- Bytes.dev: submit via site

## Email body:

Hi [name],

I wrote a step-by-step tutorial on building a "What's New" changelog modal in React using the native HTML `<dialog>` element instead of library modals like react-modal (188KB). It covers localStorage persistence, per-entry seen/unseen tracking, WCAG focus trapping, and analytics callbacks -- all in under 80 lines.

Your readers might find it useful since it uses a pattern (native `<dialog>` + headless state management) that's becoming the default for modal UIs in 2026, replacing the heavier library approach.

Link: https://tourkit.dev/blog/whats-new-modal-react

Thanks,
Domi
