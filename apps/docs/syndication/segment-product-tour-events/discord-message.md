## Channel: #articles or #show-off in Reactiflux

**Message:**

Wrote a guide on building a Segment plugin for Tour Kit that pipes product tour events to 400+ analytics destinations. About 40 lines of TypeScript. The plugin interface is the same one used for GA4, PostHog, and Mixpanel plugins, just targeting Segment's `analytics.track()` instead.

https://usertourkit.com/blog/segment-product-tour-events

If anyone's using Segment with product tours, curious how you're handling event taxonomy — Segment has zero docs for tour-specific events.
